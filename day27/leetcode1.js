function compactObject(obj) {
  let reesult = [];
  if (typeof obj === "object") {
       for(let key in obj){
          if(Array.isArray(obj.key)){
               
          
          }
       }
  }
}
obj = { a: null, b: [false, 1] };
compactObject(obj);
